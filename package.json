{
  "name": "flowtime",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "release": "npm run build && mkdir -p dist/skills && cp manifest.json src/styles.css README.md dist/ && cp -r skills/flowtime/ dist/skills/flowtime/; echo 'Release ready at dist/'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
